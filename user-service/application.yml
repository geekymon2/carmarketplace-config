server:
  port: 8084
  
eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

api-config:
  jwt-config:
    jwt-disabled: false
    jwt-secret: ${JWT_AUTH_SECRET}
    jwt-validity: 20